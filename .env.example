# Virtual Printer MCP Server - Environment Variables

# Storage Type
# Options: "file" (local development) or "vercel-kv" (Vercel deployment)
STORAGE_TYPE=file

# Web UI Server Port (local development)
WEB_UI_PORT=3001

# MCP HTTP Server Port (for n8n integration)
MCP_HTTP_PORT=3002

# Node Environment
NODE_ENV=development

# Vercel KV Configuration (only needed for Vercel deployment)
# Get these from your Vercel project dashboard
# KV_REST_API_URL=https://your-kv-instance.upstash.io
# KV_REST_API_TOKEN=your-token-here
# KV_REST_API_READ_ONLY_TOKEN=your-readonly-token-here

# Optional: Custom state file path (only used when STORAGE_TYPE=file)
# STATE_FILE_PATH=/custom/path/to/printer-state.json
